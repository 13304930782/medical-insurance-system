# 测试记录

最近验证日期：2026-08-25

执行命令：

```powershell
mvn test
```

最近一次全量结果：16 个测试类、26 个测试，0 失败、0 错误、0 跳过。

## 自动化覆盖

- 数据库原始15表字段契约，与《数据库一览表.DOC》逐表核对。
- 注册、一次性 RSA-OAEP-256 密码传输、BCrypt 存储、登录、会话、忘记密码、修改密码与操作日志。
- 新用户默认报销经办角色、登录失败锁定、重置验证码有效期与错误次数限制。
- 药品、诊疗项目、服务设施、病种、机构、单位和个人 CRUD。
- 三类待遇参数 CRUD、金额与比例校验、重复主键和区间重叠。
- 人员机构审批及药品、诊疗项目特检特治审批；不需要审批的目录项目不能进入候选或提交保存。
- 就诊与多条处方保存、后端金额重算、目录外项目标记；目录类别、收费类别和项目编码双向联动并由后端复核。
- 定点医疗机构与医院等级自动联动，后端拒绝不一致组合。
- 甲、乙、丙类项目，最高限价和医院等级限制。
- 无特检审批、审批过期、审批有效。
- 起付线、分段区间、年度封顶线。
- 预结算不累计；正式结算只累计一次。
- 取消生成负交易、恢复年度累计、防止重复取消。
- ADMIN、APPROVER、REIMBURSEMENT 接口权限隔离。
- Dashboard 年度汇总接口和 REIMBURSEMENT 角色访问。
- Dashboard 单位类型数量与百分比分布。
- 门诊历史号码候选查询、沿用提示及数据库并发安全的门诊号自动递增。
- XLS/XLSX 模板、旧版 `数据.xls` 兼容导入、导出、仅校验模式和精确确认删除。
- 统一审计日志查询及 XLSX 导出。
- AI 问答覆盖内置系统完整指南、国家医保局按问题检索、无关证据过滤、无证据拒答和 DeepSeek 未配置提示。
- Thymeleaf 首页完整渲染及 Dashboard、结算、查询、打印所需元素；项目编码清空按钮、联动筛选脚本通过页面完整性和 Node.js 语法检查。
- 静态 HTML/CSS/JavaScript 已拆分；JavaScript 进一步分为 `core` 公共层、`modules` 业务层和 `app.js` 启动入口，服务器响应压缩已启用，全部脚本通过 Node.js 语法检查。

## 人工浏览器验收路径

1. 报销员进入“报销办理 Dashboard”，切换“个人报销”，查询并选择人员。
2. 点击新增就诊资料，录入机构、医疗类别、病种、日期和住院号。
3. 选择该就诊并录入一条或多条处方，确认金额由后端重算。
4. 执行预结算并核对费用构成；预结算后“正式结算”按钮启用。
5. 正式结算并打印结算清单，核对年度累计。
6. 切换“单位报销”，选择单位及其参保人员，重复第 2～5 步。
7. 进入综合查询输入原因取消报销，确认出现负交易且年度累计恢复。

高频查询索引已核对：人员姓名、就诊人员与日期、结算住院号与结算时间、年度累计复合主键均已有索引。

## 真实运行冒烟验证

- 2026-08-22 使用最新打包 JAR 在 `127.0.0.1:18080` 临时启动；Flyway 校验、MySQL 数据源、Tomcat 和页面模板正常。
- 实际浏览器确认 `app.css`、`js/core`、`js/modules` 和 `app.js` 全部加载，登录、注册、忘记密码界面切换正常，控制台 0 个错误。
- 使用打包后的应用在 `127.0.0.1:8081` 临时启动，启动、数据源连接和角色初始化正常。
- `ADMIN` 登录后可访问综合查询接口。
- `APPROVER` 登录后访问报销结算接口返回 403。
- `REIMBURSEMENT` 登录后可访问报销结算接口。
- 临时进程已在验证完成后停止，未影响 IDEA 中运行的 `8080` 实例。
